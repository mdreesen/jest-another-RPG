module.exports = function(val1, val2) {
if(val1 === val2) {
    return true
} else {
    return false
}
};

//function randomNumber() {
    /* remember that "Math.floor" is a function that is used to return the largest integer
    that is less than or equal to a number
    */
   // "Math.random" function returns a floating point, with approximately uniform distribution over that range
   //return Math.floor(Math.random() * 9 + 1);
//};